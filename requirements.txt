# Runtime + inference dependencies for the Voxtral STT server.
#
# NOTE: GPU environments use CUDA 13 (cu130). See README/ADVANCED and scripts/main.sh
# for the exact install command.

torch==2.10.0
torchvision==0.25.0
torchaudio==2.10.0

fastapi==0.129.0
starlette==0.52.1
uvicorn[standard]==0.34.0
orjson==3.10.15
pydantic==2.12.5

huggingface_hub==0.34.0
numpy==2.2.2

# Audio decoding/processing (required for Voxtral realtime serving)
librosa==0.10.2.post1
soundfile==0.13.1
soxr==0.5.0.post1

# Model loader deps (required by Voxtral in vLLM)
mistral-common[audio]==1.9.1

# vLLM (CUDA 13 wheel pinned via GitHub release asset)
vllm @ https://github.com/vllm-project/vllm/releases/download/v0.16.0/vllm-0.16.0+cu130-cp38-abi3-manylinux_2_35_x86_64.whl
