# Runtime + inference dependencies for the Voxtral STT server.
#
# NOTE: GPU environments use CUDA 12.8 (cu128). See README/ADVANCED and scripts/main.sh
# for the exact install command.

torch==2.10.0
torchvision==0.25.0
torchaudio==2.10.0

fastapi==0.129.0
starlette==0.52.1
uvicorn[standard]==0.34.0
orjson==3.10.15
pydantic==2.12.5

huggingface_hub==0.34.0
hf_transfer==0.1.9
numpy==2.2.2

# Audio decoding/processing (required for Voxtral realtime serving)
librosa==0.10.2.post1
soundfile==0.13.1
soxr==0.5.0.post1

# Model loader deps (required by Voxtral in vLLM)
mistral-common[audio]==1.9.1

# vLLM 0.16.0 — installed separately by the Dockerfile / install scripts from the
# GitHub release wheel (no PyPI package for this version).

# flash-attn 2.8.3 — installed separately (requires --no-build-isolation to find
# torch at build time when no prebuilt wheel is available).
