# Voxtral STT API — Environment
# Copy to .env (do not commit — may contain secrets).
# See ADVANCED.md for the full list of supported environment variables.

# Required -----------------------------------------------------------------
VOXTRAL_API_KEY=secret_token

# Recommended ---------------------------------------------------------------
# HF_TOKEN=hf_...

# Server --------------------------------------------------------------------
SERVER_BIND_HOST=0.0.0.0
SERVER_PORT=8000
LOG_LEVEL=INFO

# Model ---------------------------------------------------------------------
VOXTRAL_MODEL_ID=mistralai/Voxtral-Mini-4B-Realtime-2602
VOXTRAL_TRANSCRIPTION_DELAY_MS=400
